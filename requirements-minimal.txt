# Minimal production requirements for Render deploy
streamlit>=1.28.0
pandas>=1.5.0
tabula-py>=2.8.0
openpyxl>=3.1.0
xlsxwriter>=3.0.0
PyPDF2>=3.0.0

# Optional: try to install camelot if possible, but don't fail if it doesn't work
# camelot-py[cv]>=1.0.8
# opencv-python-headless>=4.8.0